%% Task 2: Function Inputs and Outputs (calculateCircleArea)
%takes the radius of a circle as input and returns the area of the circle.

function area = calculateCircleArea(radius)
    area = pi * radius ^ 2
end 